<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper     PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="com.ujiuye.dao.MovieMapper">
    <resultMap type="com.ujiuye.daomain.Movie" id="movieMap">
        <id property="id" column="id" />
        <result property="movieName" column="name" />
        <result property="director" column="director" />
        <result property="timelength" column="timelength" />
        <result property="showdate" column="showdate" />
        <result property="description" column="description" />
        <result property="photo" column="photo" />

        <collection  property="movietypes"  ofType="com.ujiuye.daomain.Movietype">
            <id property="id" column="id" />
            <result property="movieTypeName" column="movieTypeName" />
        </collection>

        <collection  property="actors"  ofType="com.ujiuye.daomain.Actor">
            <id property="id" column="id" />
            <result property="actorName" column="actorName" />
            <result property="birthday" column="birthday" />
            <result property="sex" column="sex" />
            <result property="description" column="description" />
            <result property="photo" column="photo" />
        </collection>
    </resultMap>

    <select id="findAllMovie" resultMap="movieMap">
        select
        m.*,
        a.id aid,a.name aname,a.birthday abirthday,a.sex
        asex,a.description adescription, a.photo aphoto,
        c.id cid, c.catname catname
        from
        movie m
        inner join
        movie_actor ma
        on
        m.id=ma.movieid
        inner join
        actor a
        on
        ma.actorid=a.id
        inner join
        movie_type mc
        on
        m.id=mc.movieid
        inner join
        category c
        on mc.categoryid=c.id
    </select>

    <insert id="insertMovie" parameterType="com.ujiuye.daomain.Movie">
        insert into movie (movieName,showDate, timeLength, director,description,photo )
        values (#{movieName},#{showDate}, #{timeLength}, #{director},
        #{description}, #{photo})
        <selectKey resultType="Integer" order="AFTER" keyProperty="id">
            SELECT LAST_INSERT_ID() AS id
        </selectKey>
    </insert>

    <insert id="insertMovieAndType" parameterType="com.ujiuye.daomain.MovieAndType">
        insert into movie_type(movieId, movieTypeId) values (#{movieId}, #{movieTypeId})
    </insert>

    <!--删除电影与类型的中间表中的记录-->
    <delete id="deleteMovieAndType" >
        delete from movie_type where movieid=#{id}
    </delete>

    <!--删除电影与演员的中间表中的记录-->
    <delete id="deleteMovieActorById" >
        delete from movie_actor where movieid = #{id}
    </delete>

    <!--删除电影中的记录-->
    <delete id="deleteMovieById" >
        delete from movie where id = #{id}
    </delete>

    <!--修改电影-->
    <update id="update" parameterType="com.ujiuye.daomain.Movie">

        update movie
        set name=#{movieName}, photo=#{photo},description=#{description},showDate=#{showDate},
        timeLength=#{timeLength},director=#{director}
        where id = #{id}
    </update>
</mapper>